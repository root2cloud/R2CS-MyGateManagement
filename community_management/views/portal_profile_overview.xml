<odoo>
    <template id="portal_my_profile_combined" name="My Profile - Family, Pets &amp; Vehicles">
        <t t-call="portal.portal_layout">
            <!-- Add FontAwesome CSS -->
            <t t-set="head">
                <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css"/>
            </t>

            <t t-set="breadcrumbs">
                <li class="breadcrumb-item">
                    <a t-att-href="portal_my_home">My Account</a>
                </li>
                <li class="breadcrumb-item active">My Profile</li>
            </t>
            <style>
                :root {
                --primary: #6366f1;
                --secondary: #8b5cf6;
                --accent: #06b6d4;
                --success: #10b981;
                --warning: #f59e0b;
                --danger: #ef4444;
                --dark: #1e293b;
                --light: #f8fafc;
                --shadow-lg: 0 25px 50px -12px rgba(0, 0, 0, 0.25);
                --shadow-md: 0 10px 25px rgba(0, 0, 0, 0.1);
                --shadow-sm: 0 4px 6px rgba(0, 0, 0, 0.05);
                --radius-xl: 24px;
                --radius-lg: 20px;
                --radius-md: 16px;
                --radius-sm: 12px;
                }

                /* Base Styles */
                body {
                background: linear-gradient(135deg, #f8fafc 0%, #e2e8f0 100%);
                min-height: 100vh;
                font-family: 'Segoe UI', system-ui, -apple-system, sans-serif;
                }

                .profile-container {
                max-width: 1400px;
                margin: 0 auto;
                padding: 3rem 1.5rem;
                animation: fadeIn 0.8s ease-out;
                }

                @keyframes fadeIn {
                from {
                opacity: 0;
                transform: translateY(30px);
                }
                to {
                opacity: 1;
                transform: translateY(0);
                }
                }

                /* Navigation Section */
                .nav-section {
                margin-bottom: 4rem;
                }

                .nav-title {
                font-size: 2.2rem;
                font-weight: 800;
                color: var(--dark);
                margin-bottom: 2rem;
                text-align: center;
                position: relative;
                display: inline-block;
                left: 50%;
                transform: translateX(-50%);
                }

                .nav-title::after {
                content: '';
                position: absolute;
                bottom: -8px;
                left: 0;
                width: 100%;
                height: 3px;
                background: linear-gradient(90deg, var(--primary), var(--secondary));
                border-radius: 2px;
                }

                .nav-grid {
                display: grid;
                grid-template-columns: repeat(4, 1fr);
                gap: 1.5rem;
                margin-bottom: 2rem;
                transition: all 0.5s ease;
                }

                .nav-grid.expanded {
                grid-template-columns: repeat(4, 1fr);
                }

                .nav-card {
                background: linear-gradient(135deg, white, #f8fafc);
                border-radius: var(--radius-lg);
                padding: 1.8rem;
                text-align: center;
                text-decoration: none;
                color: var(--dark);
                box-shadow: var(--shadow-md);
                transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
                border: 1px solid rgba(0, 0, 0, 0.05);
                display: flex;
                flex-direction: column;
                align-items: center;
                gap: 1rem;
                position: relative;
                overflow: hidden;
                opacity: 1;
                transform: scale(1);
                }

                .nav-card.hidden {
                display: none;
                }

                .nav-card.fade-in {
                animation: fadeInCard 0.5s ease-out forwards;
                }

                @keyframes fadeInCard {
                from {
                opacity: 0;
                transform: translateY(20px);
                }
                to {
                opacity: 1;
                transform: translateY(0);
                }
                }

                .nav-card::before {
                content: '';
                position: absolute;
                top: 0;
                left: 0;
                right: 0;
                height: 4px;
                background: linear-gradient(90deg, var(--primary), var(--secondary));
                opacity: 0;
                transition: opacity 0.3s ease;
                }

                .nav-card:hover {
                transform: translateY(-10px);
                box-shadow: var(--shadow-lg);
                color: var(--primary);
                }

                .nav-card:hover::before {
                opacity: 1;
                }

                .nav-card:hover .nav-icon {
                transform: scale(1.2) translateY(-5px);
                }

                .nav-icon {
                width: 70px;
                height: 70px;
                background: linear-gradient(135deg, var(--primary), var(--secondary));
                border-radius: 50%;
                display: flex;
                align-items: center;
                justify-content: center;
                font-size: 1.8rem;
                color: white;
                margin-bottom: 0.5rem;
                transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
                box-shadow: 0 10px 20px rgba(99, 102, 241, 0.2);
                }

                .nav-card:hover .nav-icon {
                box-shadow: 0 15px 30px rgba(99, 102, 241, 0.3);
                }

                .nav-label {
                font-size: 1.1rem;
                font-weight: 600;
                color: var(--dark);
                transition: color 0.3s ease;
                }

                .nav-card:hover .nav-label {
                color: var(--primary);
                }

                .nav-subtitle {
                font-size: 0.9rem;
                color: #64748b;
                margin-top: 0.25rem;
                }

                /* View More Button */
                .view-more-container {
                text-align: center;
                margin-top: 2rem;
                }

                .btn-view-more {
                background: linear-gradient(135deg, var(--primary), var(--secondary));
                color: white;
                border: none;
                border-radius: var(--radius-lg);
                padding: 1rem 2.5rem;
                font-weight: 700;
                font-size: 1.1rem;
                cursor: pointer;
                transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
                display: inline-flex;
                align-items: center;
                gap: 0.8rem;
                box-shadow: 0 10px 30px rgba(99, 102, 241, 0.3);
                position: relative;
                overflow: hidden;
                z-index: 1;
                }

                .btn-view-more::before {
                content: '';
                position: absolute;
                top: 0;
                left: -100%;
                width: 100%;
                height: 100%;
                background: linear-gradient(90deg,
                transparent,
                rgba(255, 255, 255, 0.4),
                transparent);
                transition: 0.6s;
                z-index: -1;
                }

                .btn-view-more:hover {
                transform: translateY(-5px);
                box-shadow: 0 20px 40px rgba(99, 102, 241, 0.4);
                }

                .btn-view-more:hover::before {
                left: 100%;
                }

                .btn-view-more i {
                font-size: 1.2rem;
                transition: transform 0.3s ease;
                }

                .btn-view-more.expanded i {
                transform: rotate(180deg);
                }

                /* Page Header */
                .page-header {
                text-align: center;
                margin-bottom: 4rem;
                position: relative;
                }

                .page-title {
                font-size: 3.5rem;
                font-weight: 900;
                background: linear-gradient(135deg, var(--primary), var(--secondary), var(--accent));
                -webkit-background-clip: text;
                -webkit-text-fill-color: transparent;
                background-clip: text;
                margin-bottom: 1rem;
                position: relative;
                display: inline-block;
                }

                .page-title::after {
                content: '';
                position: absolute;
                bottom: -10px;
                left: 50%;
                transform: translateX(-50%);
                width: 200px;
                height: 4px;
                background: linear-gradient(90deg, var(--primary), var(--secondary));
                border-radius: 2px;
                opacity: 0.7;
                }

                .page-subtitle {
                color: #64748b;
                font-size: 1.1rem;
                max-width: 600px;
                margin: 0 auto;
                line-height: 1.6;
                }

                /* Section Styling */
                .section-wrapper {
                background: white;
                border-radius: var(--radius-xl);
                padding: 0;
                margin-bottom: 3rem;
                box-shadow: var(--shadow-lg);
                overflow: hidden;
                position: relative;
                border: 1px solid rgba(255, 255, 255, 0.2);
                backdrop-filter: blur(10px);
                }

                .section-wrapper::before {
                content: '';
                position: absolute;
                top: 0;
                left: 0;
                right: 0;
                height: 5px;
                background: var(--section-gradient, linear-gradient(90deg, var(--primary), var(--secondary)));
                z-index: 2;
                }

                .section-header {
                padding: 2.5rem 3rem;
                background: linear-gradient(135deg,
                rgba(99, 102, 241, 0.05) 0%,
                rgba(139, 92, 246, 0.05) 100%);
                border-bottom: 1px solid rgba(0, 0, 0, 0.05);
                display: flex;
                justify-content: space-between;
                align-items: center;
                flex-wrap: wrap;
                gap: 1.5rem;
                }

                .section-title {
                display: flex;
                align-items: center;
                gap: 1rem;
                margin: 0;
                }

                .section-title i {
                font-size: 2rem;
                background: var(--section-gradient, linear-gradient(135deg, var(--primary), var(--secondary)));
                -webkit-background-clip: text;
                -webkit-text-fill-color: transparent;
                background-clip: text;
                width: 60px;
                height: 60px;
                display: flex;
                align-items: center;
                justify-content: center;
                background: rgba(99, 102, 241, 0.1);
                border-radius: 50%;
                padding: 0.8rem;
                }

                .section-title h3 {
                font-size: 2.2rem;
                font-weight: 800;
                color: var(--dark);
                margin: 0;
                }

                /* Add Button */
                .btn-add {
                background: linear-gradient(135deg, var(--primary), var(--secondary));
                color: white;
                border: none;
                border-radius: var(--radius-lg);
                padding: 1rem 2.5rem;
                font-weight: 700;
                font-size: 1.1rem;
                cursor: pointer;
                transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
                text-decoration: none;
                display: inline-flex;
                align-items: center;
                gap: 0.8rem;
                box-shadow: 0 10px 30px rgba(99, 102, 241, 0.3);
                position: relative;
                overflow: hidden;
                z-index: 1;
                }

                .btn-add::before {
                content: '';
                position: absolute;
                top: 0;
                left: -100%;
                width: 100%;
                height: 100%;
                background: linear-gradient(90deg,
                transparent,
                rgba(255, 255, 255, 0.4),
                transparent);
                transition: 0.6s;
                z-index: -1;
                }

                .btn-add:hover {
                transform: translateY(-5px);
                box-shadow: 0 20px 40px rgba(99, 102, 241, 0.4);
                }

                .btn-add:hover::before {
                left: 100%;
                }

                .btn-add i {
                font-size: 1.2rem;
                }

                /* Cards Grid */
                .cards-grid {
                padding: 2.5rem 3rem;
                display: grid;
                grid-template-columns: repeat(auto-fill, minmax(350px, 1fr));
                gap: 2rem;
                }

                /* Profile Card */
                .profile-card {
                background: linear-gradient(135deg, white, #f8fafc);
                border-radius: var(--radius-lg);
                overflow: hidden;
                box-shadow: var(--shadow-md);
                transition: all 0.5s cubic-bezier(0.4, 0, 0.2, 1);
                position: relative;
                border: 1px solid rgba(0, 0, 0, 0.05);
                height: 100%;
                display: flex;
                flex-direction: column;
                }

                .profile-card:hover {
                transform: translateY(-10px);
                box-shadow: var(--shadow-lg);
                }

                .profile-card::before {
                content: '';
                position: absolute;
                top: 0;
                left: 0;
                right: 0;
                height: 4px;
                background: linear-gradient(90deg, var(--primary), var(--secondary));
                opacity: 0.8;
                }

                .card-image {
                height: 220px;
                position: relative;
                overflow: hidden;
                }

                .card-image::after {
                content: '';
                position: absolute;
                bottom: 0;
                left: 0;
                right: 0;
                height: 60%;
                background: linear-gradient(to top, rgba(0,0,0,0.3), transparent);
                pointer-events: none;
                }

                .card-image img {
                width: 100%;
                height: 100%;
                object-fit: cover;
                transition: transform 0.8s cubic-bezier(0.4, 0, 0.2, 1);
                }

                .profile-card:hover .card-image img {
                transform: scale(1.15);
                }

                .placeholder-icon {
                position: absolute;
                top: 50%;
                left: 50%;
                transform: translate(-50%, -50%);
                font-size: 5rem;
                color: rgba(99, 102, 241, 0.2);
                z-index: 1;
                }

                /* Status Badge */
                .status-badge {
                position: absolute;
                top: 1rem;
                right: 1rem;
                padding: 0.6rem 1.2rem;
                border-radius: 50px;
                font-size: 0.85rem;
                font-weight: 700;
                color: white;
                box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
                z-index: 2;
                }

                /* Card Content */
                .card-content {
                padding: 2rem;
                flex-grow: 1;
                display: flex;
                flex-direction: column;
                }

                .card-title {
                font-size: 1.8rem;
                font-weight: 800;
                color: var(--dark);
                margin: 0 0 1rem 0;
                line-height: 1.3;
                }

                .card-subtitle {
                color: #64748b;
                font-size: 1rem;
                margin-bottom: 1.5rem;
                line-height: 1.5;
                }

                /* Info Items */
                .info-grid {
                display: flex;
                flex-direction: column;
                gap: 0.8rem;
                margin-bottom: 1.5rem;
                }

                .info-item {
                display: flex;
                align-items: center;
                gap: 0.8rem;
                padding: 0.8rem;
                background: rgba(241, 245, 249, 0.5);
                border-radius: var(--radius-sm);
                transition: all 0.3s ease;
                }

                .info-item:hover {
                background: rgba(241, 245, 249, 0.9);
                transform: translateX(5px);
                }

                .info-item i {
                width: 20px;
                color: var(--primary);
                font-size: 1.1rem;
                }

                .info-item span {
                color: #475569;
                font-weight: 500;
                }

                /* Card Actions */
                .card-actions {
                margin-top: auto;
                display: flex;
                gap: 1rem;
                padding-top: 1.5rem;
                border-top: 1px solid rgba(0, 0, 0, 0.05);
                }

                .btn-action {
                flex: 1;
                padding: 0.9rem 1.5rem;
                border-radius: var(--radius-sm);
                font-weight: 600;
                font-size: 0.95rem;
                text-align: center;
                text-decoration: none;
                transition: all 0.3s ease;
                display: flex;
                align-items: center;
                justify-content: center;
                gap: 0.5rem;
                }

                .btn-edit {
                background: linear-gradient(135deg, #e0e7ff, #c7d2fe);
                color: var(--primary);
                }

                .btn-edit:hover {
                background: linear-gradient(135deg, var(--primary), var(--secondary));
                color: white;
                transform: translateY(-2px);
                }

                .btn-delete {
                background: linear-gradient(135deg, #fee2e2, #fecaca);
                color: var(--danger);
                }

                .btn-delete:hover {
                background: linear-gradient(135deg, var(--danger), #dc2626);
                color: white;
                transform: translateY(-2px);
                }

                /* Empty State */
                .empty-state {
                grid-column: 1 / -1;
                text-align: center;
                padding: 5rem 2rem;
                background: linear-gradient(135deg, #f8fafc, #e2e8f0);
                border-radius: var(--radius-lg);
                border: 2px dashed rgba(99, 102, 241, 0.2);
                }

                .empty-state i {
                font-size: 6rem;
                background: linear-gradient(135deg, var(--primary), var(--secondary));
                -webkit-background-clip: text;
                -webkit-text-fill-color: transparent;
                background-clip: text;
                margin-bottom: 1.5rem;
                opacity: 0.7;
                }

                .empty-state h4 {
                font-size: 2rem;
                color: var(--dark);
                margin-bottom: 0.5rem;
                }

                .empty-state p {
                color: #64748b;
                font-size: 1.1rem;
                max-width: 400px;
                margin: 0 auto;
                }

                /* Flat Card Special */
                .flat-main-card {
                margin: 2rem 3rem;
                background: linear-gradient(135deg, #f0f9ff, #e0f2fe);
                border-radius: var(--radius-lg);
                padding: 2.5rem;
                border: 1px solid rgba(6, 182, 212, 0.2);
                position: relative;
                overflow: hidden;
                }

                .flat-main-card::before {
                content: '';
                position: absolute;
                top: 0;
                left: 0;
                right: 0;
                bottom: 0;
                background: linear-gradient(45deg, transparent, rgba(6, 182, 212, 0.05), transparent);
                pointer-events: none;
                }

                .flat-title {
                font-size: 2.2rem;
                font-weight: 800;
                color: var(--dark);
                margin-bottom: 1rem;
                display: flex;
                align-items: center;
                gap: 1rem;
                }

                .flat-title i {
                color: var(--accent);
                }

                .flat-details {
                display: grid;
                grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
                gap: 1.5rem;
                margin: 2rem 0;
                }

                .flat-detail-item {
                background: white;
                padding: 1.2rem;
                border-radius: var(--radius-md);
                box-shadow: var(--shadow-sm);
                display: flex;
                align-items: center;
                gap: 1rem;
                transition: all 0.3s ease;
                }

                .flat-detail-item:hover {
                transform: translateY(-3px);
                box-shadow: var(--shadow-md);
                }

                .flat-detail-item i {
                width: 40px;
                height: 40px;
                background: linear-gradient(135deg, var(--accent), #0891b2);
                color: white;
                border-radius: 50%;
                display: flex;
                align-items: center;
                justify-content: center;
                font-size: 1.2rem;
                }

                /* Alert Styling */
                .alert-overdue {
                background: linear-gradient(135deg, #fef2f2, #fee2e2);
                border-left: 4px solid var(--danger);
                padding: 1.5rem 2rem;
                margin: 1.5rem 3rem;
                border-radius: var(--radius-md);
                display: flex;
                align-items: center;
                gap: 1rem;
                animation: pulse 2s infinite;
                }

                @keyframes pulse {
                0%, 100% { opacity: 1; }
                50% { opacity: 0.9; }
                }

                .alert-overdue i {
                color: var(--danger);
                font-size: 1.5rem;
                }

                .alert-overdue h5 {
                color: #7f1d1d;
                margin: 0;
                font-weight: 700;
                }

                /* Rooms Grid */
                .rooms-grid {
                margin-top: 3rem;
                }

                .rooms-title {
                font-size: 1.5rem;
                font-weight: 700;
                color: var(--dark);
                margin-bottom: 1.5rem;
                padding-bottom: 0.5rem;
                border-bottom: 2px solid rgba(0, 0, 0, 0.05);
                }

                /* Badge Variants */
                .badge {
                display: inline-block;
                padding: 0.5rem 1rem;
                border-radius: 50px;
                font-size: 0.85rem;
                font-weight: 700;
                margin: 0.25rem;
                }

                .badge-primary {
                background: linear-gradient(135deg, var(--primary), var(--secondary));
                color: white;
                }

                .badge-success {
                background: linear-gradient(135deg, var(--success), #059669);
                color: white;
                }

                .badge-warning {
                background: linear-gradient(135deg, var(--warning), #d97706);
                color: white;
                }

                .badge-danger {
                background: linear-gradient(135deg, var(--danger), #dc2626);
                color: white;
                }

                /* Responsive Design */
                @media (max-width: 1200px) {
                .cards-grid {
                grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
                }

                .nav-grid {
                grid-template-columns: repeat(3, 1fr);
                }

                .nav-grid.expanded {
                grid-template-columns: repeat(3, 1fr);
                }
                }

                @media (max-width: 768px) {
                .profile-container {
                padding: 2rem 1rem;
                }

                .page-title {
                font-size: 2.5rem;
                }

                .nav-grid {
                grid-template-columns: repeat(2, 1fr);
                }

                .nav-grid.expanded {
                grid-template-columns: repeat(2, 1fr);
                }

                .nav-icon {
                width: 60px;
                height: 60px;
                font-size: 1.5rem;
                }

                .section-header {
                padding: 2rem 1.5rem;
                flex-direction: column;
                text-align: center;
                }

                .section-title {
                justify-content: center;
                }

                .cards-grid {
                padding: 2rem 1.5rem;
                grid-template-columns: 1fr;
                }

                .flat-main-card {
                margin: 1.5rem;
                padding: 2rem;
                }

                .flat-details {
                grid-template-columns: 1fr;
                }

                .alert-overdue {
                margin: 1.5rem;
                }

                .btn-add {
                width: 100%;
                justify-content: center;
                }
                }

                @media (max-width: 480px) {
                .page-title {
                font-size: 2rem;
                }

                .nav-grid {
                grid-template-columns: 1fr;
                }

                .nav-grid.expanded {
                grid-template-columns: 1fr;
                }

                .section-title h3 {
                font-size: 1.8rem;
                }

                .card-actions {
                flex-direction: column;
                }

                .flat-title {
                font-size: 1.8rem;
                flex-direction: column;
                text-align: center;
                }
                }
            </style>
            <div class="profile-container">
                <!-- 🏠 HOME NAVIGATION SECTION -->
                <div class="nav-section">
                    <h2 class="nav-title">My Community</h2>
                    <div class="nav-grid" id="navGrid">

                        <a href="/my/visitors?state=pending" class="nav-card" title="Pending Requests">
                            <div class="nav-icon">
                                <i class="fa fa-clock"></i>
                                <!-- Pending Count Badge -->
                                <t t-if="pending_request_count and pending_request_count > 0">
                                    <span class="pending-count-badge">
                                        <t t-esc="pending_request_count"/>
                                    </span>
                                </t>
                            </div>
                            <div>
                                <div class="nav-label">Request Pending</div>
                                <div class="nav-subtitle">
                                    <t t-if="pending_request_count and pending_request_count > 0">
                                        <t t-esc="pending_request_count"/>
                                        pending
                                    </t>
                                    <t t-else="">
                                        No pending requests
                                    </t>
                                </div>
                            </div>
                        </a>
                        <!-- First 4 cards - First Row -->
                        <!-- Notice Board -->
                        <a href="/my/notices" class="nav-card" title="Notice Board">
                            <div class="nav-icon">
                                <i class="fa fa-bullhorn"></i>
                            </div>
                            <div>
                                <div class="nav-label">Notice Board</div>
                                <div class="nav-subtitle">Community Updates</div>
                            </div>
                        </a>

                        <!-- Helpdesk -->
                        <a href="/helpdesk_ticket" class="nav-card" title="Helpdesk Tickets">
                            <div class="nav-icon">
                                <i class="fa fa-headset"></i>
                            </div>
                            <div>
                                <div class="nav-label">Helpdesk</div>
                                <div class="nav-subtitle">Support Tickets</div>
                            </div>
                        </a>

                        <!-- Daily Help -->
                        <a href="/my/daily_slots" class="nav-card" title="Daily Help Services">
                            <div class="nav-icon">
                                <i class="fa fa-hands-helping"></i>

                            </div>
                            <div>
                                <div class="nav-label">Daily Help</div>
                                <div class="nav-subtitle">Service Slots</div>
                            </div>
                        </a>

                        <!-- Guest Invite -->
                        <a href="/my/guest/invites" class="nav-card" title="Guest Invitations">
                            <div class="nav-icon">
                                <i class="fa fa-user-friends"></i>
                            </div>
                            <div>
                                <div class="nav-label">Pre Approval Guest Invite</div>
                                <div class="nav-subtitle">Manage Guests</div>
                            </div>
                        </a>

                        <!-- Next 3 cards - Second Row -->
                        <!-- Party & Group Invite -->
                        <a href="/my/party/group/invite" class="nav-card" title="Party &amp; Group Invitations">
                            <div class="nav-icon">
                                <i class="fa fa-glass-cheers"></i>
                            </div>
                            <div>
                                <div class="nav-label">Pre Approval Party &amp; Group</div>
                                <div class="nav-subtitle">Event Invites</div>
                            </div>
                        </a>

                        <!-- Cab -->
                        <a href="/my/cab/preapproval" class="nav-card" title="Cab Services">
                            <div class="nav-icon">
                                <i class="fa fa-taxi"></i>
                            </div>
                            <div>
                                <div class="nav-label">Pre Approval Cab Service</div>
                                <div class="nav-subtitle">Pre-approval</div>
                            </div>
                        </a>

                        <!-- Delivery -->
                        <a href="/my/delivery-pass" class="nav-card" title="Delivery Pass">
                            <div class="nav-icon">
                                <i class="fa fa-shipping-fast"></i>
                            </div>
                            <div>
                                <div class="nav-label">Pre Approval Delivery</div>
                                <div class="nav-subtitle">Pass Management</div>
                            </div>
                        </a>

                        <!-- Hidden Cards (Will be shown on View More) -->
                        <!-- Management -->
                        <a href="/my/community_management" class="nav-card hidden" title="Community Management">
                            <div class="nav-icon">
                                <i class="fa fa-users-cog"></i>
                            </div>
                            <div>
                                <div class="nav-label">Management</div>
                                <div class="nav-subtitle">Community Settings</div>
                            </div>
                        </a>

                        <!-- Flats -->
                        <a href="/my/flats" class="nav-card hidden" title="Flats Management">
                            <div class="nav-icon">
                                <i class="fa fa-building"></i>
                            </div>
                            <div>
                                <div class="nav-label">Flats</div>
                                <div class="nav-subtitle">Properties</div>
                            </div>
                        </a>

                        <!-- Amenity Bookings -->
                        <a href="/my/amenities/bookings" class="nav-card hidden" title="Amenity Bookings">
                            <div class="nav-icon">
                                <i class="fa fa-swimming-pool"></i>
                            </div>
                            <div>
                                <div class="nav-label">Amenities</div>
                                <div class="nav-subtitle">Bookings</div>
                            </div>
                        </a>

                        <!-- Visiting Help -->
                        <a href="/my/visiting-help" class="nav-card hidden" title="Visiting Help">
                            <div class="nav-icon">
                                <i class="fa fa-hands"></i>
                            </div>
                            <div>
                                <div class="nav-label">Pre Approval Visiting Help</div>
                                <div class="nav-subtitle">Assistance Services</div>
                            </div>
                        </a>

                        <!-- Posts -->
                        <a href="/community/posts" class="nav-card hidden" title="Community Posts">
                            <div class="nav-icon">
                                <i class="fa fa-newspaper"></i>
                            </div>
                            <div>
                                <div class="nav-label">Posts</div>
                                <div class="nav-subtitle">Community Feed</div>
                            </div>
                        </a>

                        <!-- My Invoices -->
                        <a href="/my/invoices" class="nav-card hidden" title="My Invoices">
                            <div class="nav-icon">
                                <i class="fa fa-file-invoice-dollar"></i>
                            </div>
                            <div>
                                <div class="nav-label">My Invoices</div>
                                <div class="nav-subtitle">Billing &amp; Payments</div>
                            </div>
                        </a>
                    </div>

                    <!-- View More Button -->
                    <div class="view-more-container">
                        <button class="btn-view-more" id="viewMoreBtn">
                            <i class="fa fa-chevron-down"></i>
                            View More Options
                        </button>
                    </div>
                </div>

                <!-- 🏡 MY PROFILE CONTENT -->
                <div class="page-header">
                    <h1 class="page-title">My Complete Profile</h1>
                    <p class="page-subtitle">Manage your family, pets, vehicles, and flat details in one place</p>
                </div>

                <!-- 🏠 FLAT SECTION -->
                <div class="section-wrapper"
                     style="--section-gradient: linear-gradient(90deg, var(--accent), var(--primary));">
                    <div class="section-header">
                        <div class="section-title">
                            <i class="fa fa-home"></i>
                            <h3>My Flat Details</h3>
                        </div>
                    </div>

                    <!-- No Flat Assigned -->
                    <t t-if="not flats">
                        <div class="empty-state">
                            <i class="fa fa-home"></i>
                            <h4>No Flat Assigned</h4>
                            <p>Please contact your society admin to get your flat assigned.</p>
                        </div>
                    </t>

                    <!-- Single Flat -->
                    <t t-elif="len(flats) == 1">
                        <t t-foreach="flats" t-as="flat">
                            <div class="flat-main-card">
                                <div class="flat-title">
                                    <i class="fa fa-building"></i>
                                    <span>
                                        <t t-esc="flat.name"/>
                                    </span>
                                    <span t-if="flat.status == 'occupied'" class="status-badge"
                                          style="background: linear-gradient(135deg, var(--success), #059669);">
                                        <t t-esc="flat.status.title()"/>
                                    </span>
                                    <span t-else="" class="status-badge"
                                          style="background: linear-gradient(135deg, var(--warning), #d97706);">
                                        <t t-esc="flat.status.title()"/>
                                    </span>
                                </div>

                                <div class="flat-details">
                                    <div class="flat-detail-item">
                                        <i class="fa fa-building"></i>
                                        <div>
                                            <div style="font-size: 0.9rem; color: #64748b;">Building</div>
                                            <div style="font-weight: 600; color: var(--dark);">
                                                <t t-esc="flat.building_id.name"/>
                                            </div>
                                        </div>
                                    </div>

                                    <t t-if="flat.floor_id.name">
                                        <div class="flat-detail-item">
                                            <i class="fa fa-layer-group"></i>
                                            <div>
                                                <div style="font-size: 0.9rem; color: #64748b;">Floor</div>
                                                <div style="font-weight: 600; color: var(--dark);">
                                                    <t t-esc="flat.floor_id.name"/>
                                                </div>
                                            </div>
                                        </div>
                                    </t>

                                    <t t-if="flat.flat_type_id.name">
                                        <div class="flat-detail-item">
                                            <i class="fa fa-tag"></i>
                                            <div>
                                                <div style="font-size: 0.9rem; color: #64748b;">Type</div>
                                                <div style="font-weight: 600; color: var(--dark);">
                                                    <t t-esc="flat.flat_type_id.name"/>
                                                </div>
                                            </div>
                                        </div>
                                    </t>

                                    <t t-if="flat.area">
                                        <div class="flat-detail-item">
                                            <i class="fa fa-arrows-alt"></i>
                                            <div>
                                                <div style="font-size: 0.9rem; color: #64748b;">Area</div>
                                                <div style="font-weight: 600; color: var(--dark);">
                                                    <t t-esc="flat.area"/>
                                                    sq.ft
                                                </div>
                                            </div>
                                        </div>
                                    </t>

                                    <t t-if="flat.rent_price">
                                        <div class="flat-detail-item">
                                            <i class="fa fa-money-bill-wave"></i>
                                            <div>
                                                <div style="font-size: 0.9rem; color: #64748b;">Rent</div>
                                                <div style="font-weight: 600; color: var(--dark);">
                                                    <t t-esc="flat.rent_price"/>
                                                    / month
                                                </div>
                                            </div>
                                        </div>
                                    </t>

                                    <t t-if="flat.lease_start_date">
                                        <div class="flat-detail-item">
                                            <i class="fa fa-calendar-check"></i>
                                            <div>
                                                <div style="font-size: 0.9rem; color: #64748b;">Lease Period</div>
                                                <div style="font-weight: 600; color: var(--dark);">
                                                    <t t-esc="flat.lease_start_date"/>
                                                    —
                                                    <t t-esc="flat.lease_end_date or 'Ongoing'"/>
                                                </div>
                                            </div>
                                        </div>
                                    </t>

                                    <div class="flat-detail-item">
                                        <i class="fa fa-car"></i>
                                        <div>
                                            <div style="font-size: 0.9rem; color: #64748b;">Parking Slots</div>
                                            <div style="font-weight: 600; color: var(--dark);">
                                                <t t-if="flat.parking_slot_ids">
                                                    <t t-foreach="flat.parking_slot_ids" t-as="slot">
                                                        <t t-esc="slot.name"/>
                                                        <t t-if="not slot_last">,</t>
                                                    </t>
                                                </t>
                                                <t t-else="">
                                                    None Assigned
                                                </t>
                                            </div>
                                        </div>
                                    </div>
                                </div>

                                <!-- Rooms Section -->
                                <t t-if="flat.room_ids">
                                    <div class="rooms-grid">
                                        <div class="rooms-title">Rooms in this Flat</div>
                                        <div class="cards-grid">
                                            <t t-foreach="flat.room_ids" t-as="room">
                                                <div class="profile-card">
                                                    <div class="card-image">
                                                        <t t-if="room.room_image">
                                                            <img t-att-src="image_data_uri(room.room_image)"/>
                                                        </t>
                                                        <t t-else="">
                                                            <i class="fa fa-door-open placeholder-icon"></i>
                                                        </t>
                                                    </div>
                                                    <div class="card-content">
                                                        <h4 class="card-title">
                                                            <t t-esc="room.name"/>
                                                        </h4>
                                                        <t t-if="room.area">
                                                            <div class="info-item">
                                                                <i class="fa fa-arrows-alt"></i>
                                                                <span>
                                                                    <t t-esc="room.area"/>
                                                                    sq.ft
                                                                </span>
                                                            </div>
                                                        </t>
                                                    </div>
                                                </div>
                                            </t>
                                        </div>
                                    </div>
                                </t>
                            </div>
                        </t>
                    </t>
                </div>

                <!-- 👨‍👩‍👧‍👦 FAMILY SECTION -->
                <div class="section-wrapper"
                     style="--section-gradient: linear-gradient(90deg, var(--success), #059669);">
                    <div class="section-header">
                        <div class="section-title">
                            <i class="fa fa-users"></i>
                            <h3>Family Members</h3>
                        </div>
                        <a href="/my/family/new" class="btn-add">
                            <i class="fa fa-plus"></i>
                            Add Family Member
                        </a>
                    </div>

                    <div class="cards-grid">
                        <t t-if="not family_members">
                            <div class="empty-state">
                                <i class="fa fa-users"></i>
                                <h4>No Family Members Yet</h4>
                                <p>Add your family members to manage their details</p>
                            </div>
                        </t>

                        <t t-foreach="family_members" t-as="member">
                            <div class="profile-card">
                                <div class="card-image">
                                    <t t-if="member.photo">
                                        <img t-att-src="image_data_uri(member.photo)"/>
                                    </t>
                                    <t t-else="">
                                        <i class="fa fa-user placeholder-icon"></i>
                                    </t>
                                </div>
                                <div class="card-content">
                                    <h4 class="card-title">
                                        <t t-esc="member.name"/>
                                    </h4>
                                    <div class="info-grid">
                                        <t t-if="member.relationship">
                                            <div class="info-item">
                                                <i class="fa fa-heart"></i>
                                                <span>
                                                    <t t-esc="member.relationship"/>
                                                </span>
                                            </div>
                                        </t>
                                        <t t-if="member.gender">
                                            <div class="info-item">
                                                <i class="fa fa-venus-mars"></i>
                                                <span>
                                                    <t t-esc="member.gender.title()"/>
                                                </span>
                                            </div>
                                        </t>
                                    </div>
                                    <span class="badge badge-primary">
                                        <t t-esc="member.member_type.title()"/>
                                    </span>
                                    <div class="card-actions">
                                        <a t-att-href="'/my/family/%s/edit' % member.id" class="btn-action btn-edit">
                                            <i class="fa fa-edit"></i>
                                            Edit
                                        </a>
                                        <a t-att-href="'/my/family/%s/delete' % member.id" class="btn-action btn-delete"
                                           onclick="return confirm('Delete this family member?');">
                                            <i class="fa fa-trash"></i>
                                            Delete
                                        </a>
                                    </div>
                                </div>
                            </div>
                        </t>
                    </div>
                </div>

                <!-- 🐾 PETS SECTION -->
                <div class="section-wrapper"
                     style="--section-gradient: linear-gradient(90deg, var(--warning), #d97706);">
                    <div class="section-header">
                        <div class="section-title">
                            <i class="fa fa-paw"></i>
                            <h3>My Pets</h3>
                        </div>
                        <a href="/my/pets/new" class="btn-add">
                            <i class="fa fa-plus"></i>
                            Add Pet
                        </a>
                    </div>

                    <t t-if="pets and any(p.vaccination_status == 'overdue' for p in pets)">
                        <div class="alert-overdue">
                            <i class="fa fa-exclamation-triangle"></i>
                            <h5>Vaccination Overdue! Please update vaccination for:
                                <t t-foreach="pets" t-as="pet">
                                    <t t-if="pet.vaccination_status == 'overdue'">
                                        <strong>
                                            <t t-esc="pet.name"/>
                                        </strong>
                                        <t t-if="not pet_last">,</t>
                                        <t t-set="pet_last" t-value="True"/>
                                    </t>
                                </t>
                            </h5>
                        </div>
                    </t>

                    <div class="cards-grid">
                        <t t-if="not pets">
                            <div class="empty-state">
                                <i class="fa fa-paw"></i>
                                <h4>No Pets Registered</h4>
                                <p>Add your furry friends to manage their details</p>
                            </div>
                        </t>

                        <t t-foreach="pets" t-as="pet">
                            <div class="profile-card">
                                <t t-if="pet.vaccination_status == 'overdue'">
                                    <div class="status-badge badge-danger">Overdue</div>
                                </t>
                                <div class="card-image">
                                    <t t-if="pet.photo">
                                        <img t-att-src="image_data_uri(pet.photo)"/>
                                    </t>
                                    <t t-else="">
                                        <i class="fa fa-paw placeholder-icon"></i>
                                    </t>
                                </div>
                                <div class="card-content">
                                    <h4 class="card-title">
                                        <t t-esc="pet.name"/>
                                    </h4>
                                    <div class="info-grid">
                                        <t t-if="pet.breed">
                                            <div class="info-item">
                                                <i class="fa fa-dna"></i>
                                                <span>
                                                    <t t-esc="pet.breed"/>
                                                </span>
                                            </div>
                                        </t>
                                    </div>
                                    <div style="display: flex; gap: 0.5rem; margin: 1rem 0;">
                                        <span class="badge badge-primary">
                                            <t t-esc="pet.pet_type.title()"/>
                                        </span>
                                        <span t-if="pet.vaccination_status == 'up_to_date'" class="badge badge-success">
                                            Up To Date
                                        </span>
                                        <span t-elif="pet.vaccination_status == 'due_soon'" class="badge badge-warning">
                                            Due Soon
                                        </span>
                                        <span t-elif="pet.vaccination_status == 'overdue'" class="badge badge-danger">
                                            Overdue
                                        </span>
                                    </div>
                                    <div class="card-actions">
                                        <a t-att-href="'/my/pets/%s/edit' % pet.id" class="btn-action btn-edit">
                                            <i class="fa fa-edit"></i>
                                            Edit
                                        </a>
                                        <a t-att-href="'/my/pets/%s/delete' % pet.id" class="btn-action btn-delete"
                                           onclick="return confirm('Delete this pet?');">
                                            <i class="fa fa-trash"></i>
                                            Delete
                                        </a>
                                    </div>
                                </div>
                            </div>
                        </t>
                    </div>
                </div>

                <!-- 🚗 VEHICLES SECTION -->
                <div class="section-wrapper"
                     style="--section-gradient: linear-gradient(90deg, var(--primary), #4f46e5);">
                    <div class="section-header">
                        <div class="section-title">
                            <i class="fa fa-car"></i>
                            <h3>My Vehicles</h3>
                        </div>
                        <a href="/my/vehicles/new" class="btn-add">
                            <i class="fa fa-plus"></i>
                            Add Vehicle
                        </a>
                    </div>

                    <div class="cards-grid">
                        <t t-if="not vehicles">
                            <div class="empty-state">
                                <i class="fa fa-car"></i>
                                <h4>No Vehicles Registered</h4>
                                <p>Add your vehicles to manage their details</p>
                            </div>
                        </t>

                        <t t-foreach="vehicles" t-as="vehicle">
                            <div class="profile-card">
                                <div class="card-image">
                                    <t t-if="vehicle.vehicle_photo">
                                        <img t-att-src="image_data_uri(vehicle.vehicle_photo)"/>
                                    </t>
                                    <t t-else="">
                                        <i class="fa fa-car placeholder-icon"></i>
                                    </t>
                                </div>
                                <div class="card-content">
                                    <h4 class="card-title">
                                        <t t-esc="vehicle.vehicle_number"/>
                                    </h4>
                                    <div class="info-grid">
                                        <t t-if="vehicle.make or vehicle.model">
                                            <div class="info-item">
                                                <i class="fa fa-car-side"></i>
                                                <span>
                                                    <t t-esc="vehicle.make or ''"/>
                                                    <t t-if="vehicle.make and vehicle.model"></t>
                                                    <t t-esc="vehicle.model or ''"/>
                                                </span>
                                            </div>
                                        </t>
                                        <t t-if="vehicle.year">
                                            <div class="info-item">
                                                <i class="fa fa-calendar"></i>
                                                <span>
                                                    <t t-esc="vehicle.year"/>
                                                </span>
                                            </div>
                                        </t>
                                    </div>
                                    <span class="badge badge-primary">
                                        <t t-esc="vehicle.vehicle_type.title()"/>
                                    </span>
                                    <div class="card-actions">
                                        <a t-att-href="'/my/vehicles/%s/edit' % vehicle.id" class="btn-action btn-edit">
                                            <i class="fa fa-edit"></i>
                                            Edit
                                        </a>
                                        <a t-att-href="'/my/vehicles/%s/delete' % vehicle.id"
                                           class="btn-action btn-delete"
                                           onclick="return confirm('Delete this vehicle?');">
                                            <i class="fa fa-trash"></i>
                                            Delete
                                        </a>
                                    </div>
                                </div>
                            </div>
                        </t>
                    </div>
                </div>
            </div>

            <!-- JavaScript for View More functionality -->
            <script>
                document.addEventListener('DOMContentLoaded', function() {
                const viewMoreBtn = document.getElementById('viewMoreBtn');
                const navGrid = document.getElementById('navGrid');
                const hiddenCards = document.querySelectorAll('.nav-card.hidden');
                let isExpanded = false;

                viewMoreBtn.addEventListener('click', function() {
                isExpanded = !isExpanded;

                if (isExpanded) {
                // Show all hidden cards
                hiddenCards.forEach((card, index) => {
                setTimeout(() => {
                card.classList.remove('hidden');
                card.classList.add('fade-in');
                card.style.animationDelay = `${index * 0.1}s`;
                }, 50);
                });

                // Update button text and icon
                viewMoreBtn.innerHTML = '<i class="fa fa-chevron-up"></i> View Less';
                viewMoreBtn.classList.add('expanded');
                } else {
                // Hide the extra cards
                hiddenCards.forEach(card => {
                card.classList.remove('fade-in');
                card.classList.add('hidden');
                });

                // Update button text and icon
                viewMoreBtn.innerHTML = '<i class="fa fa-chevron-down"></i> View More Options';
                viewMoreBtn.classList.remove('expanded');
                }
                });
                });
            </script>
        </t>
    </template>
</odoo>

