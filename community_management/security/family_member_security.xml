<odoo>
    <data noupdate="1">
        <!-- Portal users can only see their own family members -->
        <record id="family_member_portal_rule" model="ir.rule">
            <field name="name">Portal User: Own Family Members</field>
            <field name="model_id" ref="model_family_member"/>
            <field name="domain_force">[('tenant_id', '=', user.partner_id.id)]</field>
            <field name="groups" eval="[(4, ref('base.group_portal'))]"/>
            <field name="perm_read" eval="True"/>
            <field name="perm_write" eval="True"/>
            <field name="perm_create" eval="True"/>
            <field name="perm_unlink" eval="True"/>
        </record>

        <!-- Internal users can see all family members -->
        <record id="family_member_user_rule" model="ir.rule">
            <field name="name">Internal User: All Family Members</field>
            <field name="model_id" ref="model_family_member"/>
            <field name="domain_force">[(1, '=', 1)]</field>
            <field name="groups" eval="[(4, ref('base.group_user'))]"/>
        </record>
    </data>
</odoo>
